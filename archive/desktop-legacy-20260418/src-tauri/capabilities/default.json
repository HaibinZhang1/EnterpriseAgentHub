{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "P1 Desktop command capability for Store, Adapter, and local state operations.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
