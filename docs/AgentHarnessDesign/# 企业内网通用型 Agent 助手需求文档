# 企业内网通用型 Agent 助手需求文档

## 1. 项目背景

企业开发中心日常工作涉及大量信息查询、系统操作、材料整理、知识维护、流程跟进和周期性事务处理。现有工作通常分散在多个内网系统、本地文件、知识库、会议记录、工单系统、测试系统、运维系统和管理报表中，员工需要频繁切换工具，重复执行类似操作。

本项目拟建设一个运行在企业内网环境中的通用型 Agent 助手，面向开发、测试、运维、管理人员等用户群体，帮助用户处理日常工作事务，提升信息获取、知识沉淀、任务执行和流程协同效率。

该 Agent 不以编程为核心方向，而是定位为企业内部通用型工作助手。

---

## 2. 项目定位

本项目定位为：

> 面向企业开发中心的内网通用型 Agent 助手平台，支持在无互联网访问的企业内网环境中，通过对话、浏览器操作、知识库检索、文件整理、截图归档、定时任务、偏好记忆和 Skill 沉淀等能力，协助员工完成日常工作事务，并在权限分层、沙箱隔离、审计留痕和用户可控的前提下运行。

---

## 3. 目标用户

### 3.1 开发人员

开发人员可使用 Agent 处理非编程类工作事务，例如：

- 查询项目资料、需求文档、接口说明、测试反馈；
- 整理日报、周报、项目进展；
- 跟踪需求、缺陷、工单、上线流程；
- 汇总会议纪要、评审结论、问题记录；
- 维护个人项目知识库；
- 查询企业知识库中的流程规范、操作手册、制度文档。

### 3.2 测试人员

测试人员可使用 Agent 处理测试管理和资料整理事务，例如：

- 查询测试任务、测试用例、缺陷状态；
- 汇总测试执行进度；
- 生成测试日报、测试总结、风险清单；
- 整理缺陷截图、日志、附件；
- 沉淀测试检查清单、常见问题和经验文档。

### 3.3 运维人员

运维人员可使用 Agent 处理巡检、工单、告警、知识沉淀等事务，例如：

- 查询内网监控系统、工单系统、发布系统；
- 汇总巡检结果；
- 生成值班日报、故障复盘材料；
- 整理应急预案、操作手册、常见问题；
- 跟踪告警处理状态和流程进度。

### 3.4 管理人员

管理人员可使用 Agent 进行信息汇总和管理材料生成，例如：

- 汇总项目进度；
- 汇总团队工作状态；
- 生成周报、月报、项目简报；
- 查询风险、缺陷、工单、上线计划；
- 查看企业知识库内容；
- 订阅周期性报告。

### 3.5 管理员 / 安全人员

管理员和安全人员负责 Agent 平台的治理工作，例如：

- 管理用户、角色和权限；
- 管理工具权限；
- 管理企业知识库；
- 管理 Skill 审核和发布；
- 查看审计日志；
- 配置数据保留、截图、沙箱、浏览器访问等安全策略。

---

## 4. 使用环境

### 4.1 网络环境

Agent 运行在企业内网环境中，默认无法访问互联网，但可以访问企业内部授权系统和内网链接。

Agent 应支持以下场景：

- 访问内网知识库；
- 访问内网业务系统；
- 访问内网流程系统；
- 访问内网工单系统；
- 访问内网测试、运维、管理类系统；
- 在企业内网中调用本地或内部部署的大模型能力。

### 4.2 模型环境

系统可支持本地大模型或企业内网大模型。

模型使用需要满足：

- 不将企业数据发送到互联网；
- 不依赖外部公网服务；
- 支持企业内部统一模型服务；
- 支持在权限控制和审计体系下使用模型能力。

---

## 5. 产品边界

### 5.1 系统应具备的能力

Agent 应具备以下核心能力：

- 自然语言对话；
- 工作事务协助；
- 内网系统访问；
- 浏览器点击和页面读取；
- 文件整理；
- 截图归档；
- 知识库问答；
- 个人偏好记忆；
- 定时任务；
- 经验总结；
- Skill 沉淀；
- 权限分层；
- 沙箱运行；
- 审计留痕。

### 5.2 系统不应默认承担的能力

当前需求不将以下内容作为核心方向：

- 专门面向编程的代码生成工具；
- 自动修改代码、提交代码、合并代码；
- 自动执行生产环境高危运维操作；
- 绕过企业系统权限进行操作；
- 无审核地自动生成并发布可执行 Skill；
- 无用户授权地扫描全盘文件；
- 无限制地读取用户浏览器数据、密码、Cookie 或历史记录；
- 无确认地删除文件、提交表单、审批流程或修改业务数据。

---

## 6. 核心功能需求

## 6.1 对话助手

Agent 应提供自然语言交互能力，用户可以通过对话方式提出需求。

### 6.1.1 基础对话能力

Agent 应支持：

- 理解用户自然语言指令；
- 根据上下文连续对话；
- 对用户问题进行解释、总结、归纳；
- 生成日报、周报、会议纪要、总结报告、待办清单等文本材料；
- 根据用户要求调整输出格式；
- 支持用户上传文本、文件、截图等材料辅助处理。

### 6.1.2 任务理解能力

Agent 应能够理解用户任务目标，并在必要时拆解任务步骤。

示例：

- “帮我整理今天的工作内容，生成日报草稿。”
- “帮我查一下我名下有哪些待处理工单。”
- “帮我把这些会议纪要整理进项目知识库。”
- “每周五帮我生成项目周报草稿。”
- “帮我进入缺陷系统看一下还有哪些高优先级问题。”

### 6.1.3 结果输出能力

Agent 应支持输出：

- 普通文本；
- Markdown 文档；
- 表格；
- 摘要；
- 清单；
- 报告草稿；
- 工作计划；
- 待办事项；
- 风险列表；
- 知识库文档；
- 会话总结。

---

## 6.2 内网浏览器操作

Agent 应能够在受控环境中操作浏览器，访问企业内网系统。

### 6.2.1 页面访问

Agent 应支持：

- 打开用户指定的内网链接；
- 访问企业内部业务系统；
- 读取页面内容；
- 理解页面中的表格、按钮、输入框、筛选条件等信息；
- 提取页面中的结构化数据；
- 根据页面内容生成摘要或报告。

### 6.2.2 页面操作

Agent 应支持在用户授权下执行：

- 点击按钮；
- 输入内容；
- 选择下拉项；
- 搜索和筛选；
- 翻页；
- 下载附件；
- 上传附件；
- 截取页面截图；
- 保存页面信息；
- 汇总页面数据。

### 6.2.3 浏览器操作边界

浏览器操作应受权限控制。

对于可能产生业务影响的动作，应要求用户确认，例如：

- 提交表单；
- 创建工单；
- 修改状态；
- 上传正式材料；
- 删除记录；
- 发起审批；
- 通过审批；
- 驳回流程；
- 发布内容。

Agent 不应在用户不知情的情况下自动执行高风险浏览器操作。

---

## 6.3 定时任务

Agent 应支持用户创建周期性任务，用于自动执行查询、整理、提醒、汇总等工作。

### 6.3.1 定时任务类型

定时任务可包括：

- 每日生成日报草稿；
- 每周生成周报草稿；
- 定期查询工单状态；
- 定期检查缺陷状态；
- 定期汇总项目进展；
- 定期整理知识库；
- 定期生成巡检报告；
- 定期提醒用户处理待办事项；
- 定期同步或检查指定内网页面内容。

### 6.3.2 任务配置内容

每个定时任务应包含：

- 任务名称；
- 任务描述；
- 执行时间；
- 执行周期；
- 执行范围；
- 使用的知识库；
- 使用的工具；
- 访问的系统或页面；
- 输出结果形式；
- 结果保存位置；
- 通知方式；
- 是否需要用户确认；
- 失败后的提示方式。

### 6.3.3 定时任务限制

定时任务不应默认执行高风险动作。

默认适合自动执行的任务包括：

- 查询；
- 汇总；
- 摘要；
- 生成草稿；
- 提醒；
- 保存报告；
- 生成待办。

默认不应自动执行的任务包括：

- 删除文件；
- 提交表单；
- 审批流程；
- 修改业务状态；
- 批量修改数据；
- 执行生产环境操作。

---

## 6.4 个人偏好与记忆

Agent 应支持记录用户长期稳定的偏好，并提供可视化管理能力。

### 6.4.1 记忆内容

Agent 可记录的内容包括：

- 用户常用输出格式；
- 用户日报、周报格式偏好；
- 用户常用项目名称；
- 用户常用知识库位置；
- 用户常用内网系统入口；
- 用户对语言风格的偏好；
- 用户对报告结构的偏好；
- 用户常用任务流程；
- 用户明确要求长期保存的信息。

### 6.4.2 记忆管理

用户应能够通过界面管理自己的记忆内容，包括：

- 查看已保存记忆；
- 新增记忆；
- 修改记忆；
- 删除记忆；
- 禁用记忆；
- 清空全部记忆；
- 设置哪些内容不允许自动记忆；
- 查看 Agent 在回答中使用了哪些记忆。

### 6.4.3 记忆边界

Agent 不应默认长期保存以下内容：

- 临时任务信息；
- 一次性上下文；
- 敏感账号密码；
- 个人隐私信息；
- 未经用户确认的敏感业务信息；
- 企业明确禁止保存的信息。

用户应始终拥有对个人记忆的查看、修改和删除权。

---

## 6.5 经验总结与 Skill 沉淀

Agent 应具备从重复任务中总结经验的能力，并能够将成熟经验沉淀为可复用流程或 Skill。

### 6.5.1 经验总结

Agent 可在任务完成后总结：

- 本次任务目标；
- 执行步骤；
- 使用的系统；
- 使用的知识库；
- 生成的结果；
- 用户认可的输出格式；
- 遇到的问题；
- 下次可复用的经验。

### 6.5.2 流程模板

当用户多次执行相似任务时，Agent 可建议保存为流程模板。

流程模板可包括：

- 模板名称；
- 适用场景；
- 输入内容；
- 执行步骤；
- 使用工具；
- 输出格式；
- 注意事项；
- 是否允许定时执行；
- 是否需要用户确认。

### 6.5.3 Skill

当流程模板稳定后，可进一步形成 Skill。

Skill 应包含：

- Skill 名称；
- Skill 描述；
- 适用人群；
- 适用场景；
- 输入参数；
- 执行步骤；
- 可调用工具；
- 权限要求；
- 风险等级；
- 输出格式；
- 错误处理方式；
- 使用说明；
- 版本信息；
- 审核状态；
- 发布范围。

### 6.5.4 Skill 边界

Agent 可以建议生成 Skill，但不应绕过用户确认或管理员审核自动发布可执行 Skill。

企业级 Skill 应具备审核、版本、权限和审计机制。

---

## 6.6 工具调用与权限分层

Agent 应支持调用工具，但所有工具都必须受权限控制。

### 6.6.1 工具类型

工具可包括：

- 文本处理工具；
- 知识库检索工具；
- 文件读取工具；
- 文件写入工具；
- 文件整理工具；
- 浏览器操作工具；
- 截图工具；
- 定时任务工具；
- 内网系统查询工具；
- 通知工具；
- 企业自定义工具。

### 6.6.2 权限分层

工具权限应按风险程度分层：

| 等级 | 工具类型 | 示例 |
|---|---|---|
| L0 | 纯文本处理 | 总结、改写、格式转换 |
| L1 | 只读查询 | 查询知识库、读取授权文件、读取内网页面 |
| L2 | 低风险写入 | 保存报告、生成 Markdown、整理授权目录 |
| L3 | 浏览器交互 | 点击、输入、下载、上传、截图 |
| L4 | 业务状态变更 | 提交表单、创建工单、修改状态 |
| L5 | 高风险操作 | 删除数据、批量修改、生产操作、系统命令 |

### 6.6.3 用户确认

以下操作应要求用户确认：

- 修改文件；
- 删除文件；
- 批量移动文件；
- 上传文件；
- 提交表单；
- 修改业务数据；
- 发起流程；
- 审批流程；
- 执行高风险工具；
- 发布 Skill；
- 修改知识库正式内容。

---

## 6.7 沙箱运行

Agent 应运行在沙箱环境中，避免越权访问和不可控操作。

### 6.7.1 文件沙箱

文件访问应满足：

- 默认只访问用户授权目录；
- 不默认扫描全盘；
- 不默认访问系统目录；
- 不默认访问凭证目录；
- 不默认访问浏览器密码、Cookie、历史记录；
- 删除文件前必须展示清单并确认；
- 文件清理应支持恢复或撤销；
- 文件操作应记录日志。

### 6.7.2 浏览器沙箱

浏览器操作应满足：

- 使用独立的 Agent 浏览器环境；
- 与用户日常浏览器隔离；
- 不默认读取用户个人浏览器数据；
- 支持内网访问范围控制；
- 支持截图开关；
- 支持下载目录隔离；
- 支持会话结束后的临时数据清理；
- 支持对敏感页面进行访问限制。

### 6.7.3 工具沙箱

工具调用应满足：

- 工具必须注册后才能使用；
- 工具必须声明权限等级；
- 工具调用必须记录日志；
- 高风险工具默认禁用；
- Skill 不能调用超出授权范围的工具；
- 定时任务调用工具时同样需要遵守权限策略。

---

## 6.8 本地文件清理与整理

Agent 应支持在用户授权范围内整理本地文件。

### 6.8.1 文件整理能力

Agent 可帮助用户：

- 整理下载目录；
- 整理截图目录；
- 整理会议材料；
- 整理项目资料；
- 按项目、日期、类型归档文件；
- 查找重复文件；
- 查找大文件；
- 查找长期未使用文件；
- 将会话生成结果归档；
- 将文档转换为统一格式。

### 6.8.2 文件清理限制

文件清理必须满足：

- 清理前展示文件清单；
- 用户确认后再执行；
- 不默认永久删除；
- 优先移动到回收站或隔离区；
- 支持撤销或恢复；
- 支持排除目录；
- 支持按文件类型、大小、时间筛选；
- 操作过程应记录审计日志。

---

## 6.9 截图与会话资料管理

Agent 应支持截图跟随会话存储，形成完整任务记录。

### 6.9.1 会话资料内容

每次会话可保存：

- 用户输入；
- Agent 回复；
- 执行步骤；
- 调用工具记录；
- 访问页面记录；
- 截图；
- 下载文件；
- 上传文件记录；
- 生成文档；
- 操作结果；
- 用户确认记录；
- 错误信息。

### 6.9.2 截图能力

Agent 应支持：

- 用户手动截图；
- 浏览器操作自动截图；
- 关键步骤截图；
- 页面状态截图；
- 截图归档到会话；
- 截图用于生成报告；
- 截图关联任务结果。

### 6.9.3 截图管理

截图管理应支持：

- 开启或关闭自动截图；
- 设置截图保存周期；
- 删除截图；
- 导出截图；
- 对敏感截图进行标记；
- 对敏感内容进行脱敏；
- 限制管理员查看范围；
- 按会话、任务、时间检索截图。

---

## 6.10 个人知识库

Agent 应支持维护用户个人知识库。

### 6.10.1 知识库内容

个人知识库可包含：

- Markdown 文档；
- 项目笔记；
- 会议纪要；
- 工作总结；
- 个人经验；
- 操作记录；
- 问题复盘；
- 常用模板；
- 个人待办；
- 参考资料。

### 6.10.2 知识库能力

Agent 应支持：

- 读取个人知识库；
- 检索个人知识库；
- 基于个人知识库回答问题；
- 引用知识来源；
- 帮助用户整理知识库；
- 将会话结果保存为知识库文档；
- 合并相似文档；
- 发现重复内容；
- 标记过期内容；
- 按项目、标签、时间进行组织。

### 6.10.3 知识库修改边界

Agent 修改个人知识库前应获得用户确认。

修改行为包括：

- 新增文档；
- 修改文档；
- 删除文档；
- 移动文档；
- 合并文档；
- 重命名文档；
- 批量整理目录结构。

---

## 6.11 企业知识库

Agent 应支持访问和使用企业知识库。

### 6.11.1 企业知识库内容

企业知识库可包含：

- 制度规范；
- 操作手册；
- 流程文档；
- 项目资料；
- 测试规范；
- 运维手册；
- 应急预案；
- 管理制度；
- 培训材料；
- 常见问题；
- 标准模板。

### 6.11.2 企业知识库管理

企业知识库应支持：

- 管理员创建知识库；
- 管理员维护知识库；
- 多知识库管理；
- 文档上传；
- 文档更新；
- 文档版本管理；
- 文档发布范围控制；
- 按部门、角色、项目授权；
- 标记过期文档；
- 用户反馈答案问题；
- 管理员审核和修正文档。

### 6.11.3 企业知识库使用

Agent 使用企业知识库时应满足：

- 按用户权限检索；
- 不越权访问文档；
- 回答时显示引用来源；
- 区分个人知识和企业知识；
- 对过期内容进行提示；
- 对不确定内容进行说明；
- 不擅自修改企业知识库内容。

---

## 7. 管理端需求

系统应提供管理端，用于统一治理 Agent 能力。

### 7.1 用户管理

管理端应支持：

- 创建用户；
- 禁用用户；
- 管理用户角色；
- 管理用户所属部门；
- 管理用户权限；
- 查看用户使用情况。

### 7.2 角色与权限管理

管理端应支持：

- 配置普通用户权限；
- 配置管理员权限；
- 配置部门权限；
- 配置工具权限；
- 配置知识库权限；
- 配置 Skill 使用权限；
- 配置定时任务权限；
- 配置文件访问策略；
- 配置浏览器访问策略。

### 7.3 工具管理

管理端应支持：

- 注册工具；
- 禁用工具；
- 配置工具权限等级；
- 配置工具可用范围；
- 查看工具调用记录；
- 管理高风险工具；
- 设置工具使用审批规则。

### 7.4 Skill 管理

管理端应支持：

- 查看 Skill；
- 审核 Skill；
- 发布 Skill；
- 下架 Skill；
- 管理 Skill 版本；
- 管理 Skill 权限；
- 查看 Skill 使用记录；
- 处理高风险 Skill。

### 7.5 知识库管理

管理端应支持：

- 创建企业知识库；
- 上传知识文档；
- 更新知识文档；
- 删除或归档文档；
- 设置知识库权限；
- 查看知识库使用情况；
- 处理用户反馈；
- 标记过期文档。

### 7.6 审计管理

管理端应支持查看：

- 用户会话记录；
- 工具调用记录；
- 文件访问记录；
- 文件修改记录；
- 浏览器访问记录；
- 截图记录；
- 定时任务执行记录；
- 知识库访问记录；
- Skill 执行记录；
- 高风险操作记录；
- 用户确认记录；
- 错误和异常记录。

---

## 8. 安全与合规需求

## 8.1 权限控制

系统应确保：

- 用户只能访问自己有权限的数据；
- Agent 不能绕过企业权限体系；
- 工具调用受权限限制；
- 知识库访问受权限限制；
- 浏览器访问受权限限制；
- 文件访问受授权目录限制；
- 定时任务不能突破用户权限；
- Skill 不能突破授权范围。

## 8.2 用户确认

高风险操作必须要求用户确认。

高风险操作包括但不限于：

- 删除文件；
- 批量修改文件；
- 提交表单；
- 修改业务状态；
- 上传文件到业务系统；
- 发起审批；
- 审批通过或驳回；
- 删除业务数据；
- 调用高风险工具；
- 发布或更新可执行 Skill。

## 8.3 审计留痕

系统应对 Agent 的关键行为进行审计。

审计内容包括：

- 操作用户；
- 操作时间；
- 会话内容；
- 执行目标；
- 调用工具；
- 访问文件；
- 修改文件；
- 访问页面；
- 截图记录；
- 知识库引用；
- 用户确认；
- 执行结果；
- 错误信息。

## 8.4 数据保护

系统应保护以下数据：

- 用户个人知识库；
- 企业知识库；
- 会话记录；
- 截图；
- 文件内容；
- 工具参数；
- 浏览器操作记录；
- 定时任务结果；
- 用户偏好记忆；
- Skill 执行记录。

系统应支持：

- 数据访问控制；
- 数据保留策略；
- 敏感信息脱敏；
- 日志权限控制；
- 截图权限控制；
- 用户个人数据删除；
- 企业数据隔离。

---

## 9. 典型业务场景

## 9.1 生成日报

用户输入：

> 帮我生成今天的日报。

Agent 可执行：

- 汇总用户当天会话记录；
- 查询用户授权系统中的任务状态；
- 读取个人知识库中的项目记录；
- 按用户偏好生成日报草稿；
- 保存到用户指定目录；
- 等待用户确认后再执行后续操作。

## 9.2 查询待处理工单

用户输入：

> 帮我查一下我名下还有哪些待处理工单。

Agent 可执行：

- 打开内网工单系统；
- 读取当前用户相关工单；
- 汇总待处理事项；
- 按优先级、时间、状态分类；
- 生成待办清单；
- 保存关键页面截图。

## 9.3 生成测试日报

用户输入：

> 帮我汇总今天的测试进度。

Agent 可执行：

- 查询测试管理系统；
- 查询缺陷系统；
- 汇总测试执行情况；
- 汇总新增缺陷和阻塞问题；
- 生成测试日报草稿；
- 提示风险和待跟进事项。

## 9.4 运维巡检汇总

用户输入：

> 帮我整理今天的巡检结果。

Agent 可执行：

- 访问授权的监控或巡检页面；
- 获取关键指标；
- 汇总异常项；
- 生成巡检报告；
- 保存截图和报告；
- 提醒用户关注风险。

## 9.5 会议纪要入库

用户输入：

> 把这份会议纪要整理到我的项目知识库里。

Agent 可执行：

- 读取会议纪要；
- 提取会议主题、时间、参会人、结论、待办；
- 转换为统一 Markdown 格式；
- 建议保存路径；
- 用户确认后写入个人知识库。

## 9.6 企业知识库问答

用户输入：

> 公司发布流程是怎样的？

Agent 可执行：

- 检索企业知识库；
- 根据用户权限读取相关文档；
- 生成流程说明；
- 引用来源文档；
- 标记可能过期或不确定的信息。

## 9.7 定时生成周报

用户配置：

> 每周五帮我生成项目周报草稿。

Agent 可执行：

- 定期汇总项目资料；
- 查询任务和工单状态；
- 读取个人知识库；
- 生成周报草稿；
- 通知用户查看；
- 等待用户确认后再提交或发送。

## 9.8 沉淀流程模板

用户多次执行类似任务后，Agent 可提示：

> 你多次执行“查询缺陷系统并生成测试日报”的任务，是否保存为个人流程模板？

用户确认后，Agent 可保存为可复用流程模板。

---

## 10. 关键约束

### 10.1 内网约束

系统必须适应企业内网环境：

- 不依赖互联网；
- 可访问授权内网系统；
- 可使用本地或内网模型；
- 可使用企业内部知识库；
- 可与企业内部认证和权限体系配合；
- 不将企业数据发送到外部网络。

### 10.2 安全约束

系统必须满足：

- 权限分层；
- 沙箱运行；
- 操作审计；
- 高风险操作确认；
- 浏览器隔离；
- 文件访问隔离；
- 知识库权限隔离；
- 截图和日志受控；
- Skill 审核和权限声明。

### 10.3 用户可控约束

用户应始终能够控制：

- Agent 能访问哪些文件；
- Agent 能使用哪些知识库；
- Agent 记住了哪些偏好；
- Agent 是否自动截图；
- Agent 是否执行定时任务；
- Agent 是否修改文件；
- Agent 是否提交表单；
- Agent 是否保存任务经验；
- Agent 是否生成流程模板或 Skill。

### 10.4 企业治理约束

企业应能够统一管理：

- 用户权限；
- 工具权限；
- 知识库权限；
- Skill 发布；
- 审计日志；
- 数据保留；
- 高风险能力；
- 浏览器访问范围；
- 文件访问范围；
- 模型使用范围。

---

## 11. 非目标

当前需求暂不包含以下内容：

- 具体技术架构；
- 具体技术选型；
- 数据库设计；
- 接口设计；
- 页面原型；
- 任务阶段规划；
- P0/P1/P2 拆分；
- 开发排期；
- 部署方案；
- 成本估算；
- 具体模型选型；
- 具体浏览器自动化实现；
- 具体沙箱实现方式；
- 具体 Skill 执行引擎实现。

---

## 12. 总体需求总结

本项目需要建设一个企业内网通用型 Agent 助手，服务于开发中心的开发、测试、运维、管理等人员。该 Agent 应能够通过自然语言对话帮助用户处理日常工作事务，包括内网系统查询、浏览器操作、知识库问答、文件整理、截图归档、定时任务、日报周报生成、会议纪要整理、个人偏好记忆和经验沉淀。

系统需要同时支持个人知识库和企业知识库。个人知识库由用户自行维护，企业知识库由管理员统一管理，并按部门、角色、项目等维度控制访问权限。Agent 在回答中应能够引用知识来源，并区分个人知识和企业知识。

Agent 应具备一定的自进化能力，能够从用户重复执行的任务中总结经验，形成流程模板，并在用户确认和管理员审核后沉淀为可复用 Skill。但 Agent 不应绕过审核自动发布可执行 Skill。

由于 Agent 具备浏览器操作、文件访问、截图、定时任务和工具调用能力，系统必须内置权限分层、沙箱隔离、审计留痕、用户确认、数据保护和管理员治理机制。所有高风险操作都应受到限制，不能默认自动执行。

整体上，该系统不是一个编程 Agent，而是一个面向企业内网工作场景的通用型智能助手，目标是在安全、可控、可审计的前提下，帮助员工减少重复劳动，提升信息整理、知识利用和工作流程处理效率。